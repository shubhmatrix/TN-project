Thanks for checking, Eric. package.json is getting ignored due to a global .gitignore rule, not the frontend one. I’m updating it and will push package.json shortly so the app can be built and run locally.