Thanks for flagging this, Eric. package.json was accidentally ignored due to a .gitignore rule. I’ve fixed it and will push the file shortly so the app can be built and run locally (npm install → npm run dev).